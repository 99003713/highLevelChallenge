export const invalidRequest = { statusCode: 400, message: "Invalid request." }
export const missingDate = { statusCode: 401, message: "Date is required." }
export const eventAlreadyExists = { statusCode: 422, message: "Event already exists for the requested time." }